{
  "name": "hexatone",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Isomorphic hex keyboard with arbitrary equal-division tunings and scale masking",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "check": "tsc --noEmit",
    "serve": "python3 -m http.server 8080",
    "dev": "npm run build && npm run serve",
    "test": "npm run build && node --test test/theory.test.mjs && node test/smoke.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^5.6.3"
  }
}
